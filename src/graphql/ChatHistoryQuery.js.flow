// @flow strict

export type ChatHistoryQuery$chat$history$user = {|
  +__typename: 'ChatUser',
  +name: string,
  +color: string,
|}

export type ChatHistoryQuery$chat$history$replies$user = {|
  +__typename: 'ChatUser',
  +name: string,
  +color: string,
|}

export type ChatHistoryQuery$chat$history$replies = {|
  +__typename: 'ChatMessageReply',
  +ts: string,
  +text: string,
  +username: string | null,
  +user: ChatHistoryQuery$chat$history$replies$user,
|}

export type ChatHistoryQuery$chat$history = {|
  +__typename: 'ChatMessage',
  +ts: string,
  +text: string,
  +username: string | null,
  +user: ChatHistoryQuery$chat$history$user,
  +replies: $ReadOnlyArray<ChatHistoryQuery$chat$history$replies> | null,
|}

export type ChatHistoryQuery$chat = {|
  +__typename: 'Chat',
  +history: $ReadOnlyArray<ChatHistoryQuery$chat$history>,
|}

export type ChatHistoryQuery = {|
  +chat: ChatHistoryQuery$chat,
|}
