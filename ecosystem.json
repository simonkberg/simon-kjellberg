{
  "apps": [{
    "name": "APP",
    "script": "bin/www",
    "instances": 2,
    "exec_mode": "cluster_mode",
    "autorestart": true,
    "env": {
      "PORT": 3000
    }
  }],

  "deploy": {
    "production": {
      "user": "node",
      "host": "188.166.5.14",
      "ref": "origin/master",
      "repo": "git@github.com:simonkberg/simon-kjellberg.git",
      "path": "/var/www/production",
      "post-deploy": "npm update && pm2 startOrRestart ecosystem.json --env production",
      "env": {
        "NODE_ENV": "production"
      }
    }
  }
}
